# Docker Compose Worker Configuration
# Copy this file to .env and adjust values as needed

# Script Configuration
# Specify the Python script to run (should be in app/src/)
SCRIPT_NAME=worker_script.py

# Alternative: Download script from URL (leave SCRIPT_NAME empty if using this)
# SCRIPT_URL=https://example.com/scripts/my_automation.py

# Runtime Configuration
# Maximum hours before forcing container restart (default: 3)
MAX_RUN_HOURS=3

# Display Configuration (for GUI-based automation)
# Set to 1 to enable, 0 to disable (disabled by default for better performance)
USE_XVFB=0
USE_OPENBOX=0
USE_VNC=0
USE_NOVNC=0
USE_SCREEN_RECORDING=0

# Screen resolution (if XVFB is enabled)
SCREEN_WIDTH=1366
SCREEN_HEIGHT=768

# Timezone
TZ=America/Sao_Paulo

# Python Configuration
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1
